export const Keys = {
  changedFiles: 'changed.files',
  repoPackages: 'repo.packages',
  repoDeps: 'repo.deps',
  affectedPackages: 'affected.packages',
  buildPlan: 'build.plan',
  testPlan: 'test.plan'
} as const;

